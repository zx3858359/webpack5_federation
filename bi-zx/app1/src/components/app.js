import React from 'react';

class Button extends React.Component {
    render() {
        const style = {
            background: '#800',
            color: '#fff',
            padding: 12,
          };
        return <div>这是底部</div>
    }
}

export default Button;
